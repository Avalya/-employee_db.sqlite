# Employee Database Management System

A Python application that manages employee data in an SQLite database with full CRUD (Create, Read, Update, Delete) functionality.

## Features

- Create new employee records
- Retrieve employee records by ID or all employees
- Update existing employee information
- Delete employee records
- SQLite database backend

## Requirements

- Python 3.x
- SQLite3 (included in Python standard library)

## Installation

1. Clone the repository:
   ```bash
   git clone https://github.com/yourusername/employee-database.git
   cd employee-database
# emplyoee_db
